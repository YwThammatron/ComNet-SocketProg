- ก่อน test ต้องใช้โปรแกรม clumsy 0.3 พร้อมตั้งค่า Preset เป็น localhost ipv4 udp
- ใน clumsy 0.3 ภายใต้ functions 
-- lag = packet rtt
-- drop = packet loss
-- duplicate = packet duplicate

- ip 127.0.0.1 (loopback) กับ port อะไรก็ได้ ตัวอย่างเช่น 12345
- เวลา test ให้ใช้ชื่อไฟล์ test_file.bin (binary file) ในการ run ฝั่ง client
- เมื่อ Program ทำงานเสร็จสิ้น server จะเขียนข้อมูลไฟล์ทับไฟล์ receive_file.bin ไปเรื่อยๆ
- ถ้าจะดูว่าไฟล์ที่ server รับมากับไฟล์ดั้งเดิมเหมือนกันมั้ยให้ run checkfile.py ดูผลลัพธ์ใน terminal
- ถ้าสมมุติว่าอยาก implement ให้ไฟล์ที่ถูกเขียนทับไปอยู่ path อื่นให้ไปแก้ตัวแปร file2 ใน checkfile.py
